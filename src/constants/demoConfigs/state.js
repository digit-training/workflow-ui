export const state = {
    sla: Number,
    state: String,
    applicationStatus: String,
    docUploadRequired: Boolean,
    isStartState: Boolean,
    isTerminateState: Boolean,
    actions:[
        {
            "action": String,
        }
    ]
}