export const state = {
    sla: String,
    state: String,
    applicationStatus: String,
    docUploadRequired: Boolean,
    isStartState: Boolean,
    isTerminatedState: Boolean,
    actions:[
        {
            "action": String,
        }
    ]
}